name: Pytest

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  pytest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      # - name: Set up Python
      #   uses: actions/setup-python@v2
      #   with:
      #     python-version: '3.9'
      - name: Print Changed Files
        run: |
          if git diff --name-only HEAD~1 | grep setup.py; then
            echo "File setup.py has changed"
          else
            echo "File setup.py has not changed"
          fi
      # - name: Install dependencies
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install pytest
      #     if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # - name: Run pytest check env sushi go
      #   run: pytest -q -s --name SushiGo  

